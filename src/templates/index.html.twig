{% include 'header.html.twig' %}
<div id='pageContainer'>
<h1>{{pageTitle}}</h1>

{% for post in PostArray %}
<div id='post'>
    <img src="{{ post.getFileName() }}">
    <h6> Tytuł: {{ post.gettext() }} </h6>
    <h6> Data dodania: {{ post.getTimeStamp() }} </h6>
    </div>
{% endfor %}
</div>

{% include 'footer.html.twig' %} 